[project]
name = "drf-proy-nuevo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Lucia Agostini", email = "l.agostini@itecriocuarto.org.ar" }
]
requires-python = ">=3.12"
dependencies = [
    "django>=6.1",
    "djangorestframework>=3.18.0",
]

[project.scripts]
drf-proy-nuevo = "drf_proy_nuevo:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
